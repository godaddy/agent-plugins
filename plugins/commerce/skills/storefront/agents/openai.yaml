interface:
  display_name: "Commerce Storefront"
  short_description: "Build safe, usable commerce storefronts"
  default_prompt: "Use $storefront to build a production-ready commerce storefront."
  brand_color: "#111111"
dependencies:
  tools:
    - type: "mcp"
      value: "commerce"
      description: "GoDaddy Commerce MCP server"
      transport: "streamable_http"
      url: "https://mcp.commerce.api.godaddy.com/mcp"
