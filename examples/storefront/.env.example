# fixture is deterministic and visibly labeled; mcp never falls back to fixtures.
COMMERCE_DATA_MODE=fixture
COMMERCE_MCP_TOKEN=
COMMERCE_STORE_ID=

# demo moves no money. disabled prevents session creation.
CHECKOUT_MODE=demo
PORT=3001
